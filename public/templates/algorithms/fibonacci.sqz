// Function to generate Fibonacci sequence up to n numbers in sQeeZ

fn fib(n) {
  var a = 1, b = 1;

  for (var i = 0; i < n; i++) {
    log(a);
    var tmp = a + b;
    a = b;
    b = tmp;
  }
}

fib(10); 
